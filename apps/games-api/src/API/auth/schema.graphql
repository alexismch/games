type UserRegistered {
   id: ID!
}

type Mutation {
   login(login: String!, password: String!): Boolean
   register(
      email: String!
      username: String!
      password: String!
   ): UserRegistered!
}
