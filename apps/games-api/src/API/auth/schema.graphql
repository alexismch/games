type AuthResponse {
   type: String!
   accessToken: String
   expiresIn: Int
}

type UserRegistered {
   id: ID!
}

type Mutation {
   login(login: String!, password: String!): AuthResponse
   register(
      email: String!
      username: String!
      password: String!
   ): UserRegistered!
}
